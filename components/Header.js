import { Container } from "./Container";
import styles from "./Header.module.scss";

const Header = () => {
  return <header className={styles.header}>Logo and nav</header>; //fix--> need header
};
export default Header;
